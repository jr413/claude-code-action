name: RDD Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.11

      - name: Install dependencies
        run: bun install

      - name: Run linting  
        run: bun run format:check

      - name: Run type checking  
        run: bun run typecheck

      - name: Run unit tests
        run: bun run test

      - name: Run integration tests
        run: bun run test

      - name: Generate coverage report
        run: echo "Coverage report skipped"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella

      - name: Check coverage threshold
        run: |
          COVERAGE=$(npx nyc report --reporter=text-summary | grep 'Lines' | awk '{print $3}' | sed 's/%//')
          if (( $(echo "$COVERAGE < 85" | bc -l) )); then
            echo "Coverage $COVERAGE% is below 85% threshold"
            exit 1
          fi

  # E2E tests disabled - this is a GitHub Action, not a web application
  # e2e:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - name: Skip E2E tests
  #       run: echo "E2E tests not applicable for GitHub Actions"
